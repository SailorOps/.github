name: Project Automation
on:
  issues:
    types: [opened, labeled]
  pull_request:
    types: [opened, labeled]

permissions:
  repository-projects: write
  issues: write
  pull-requests: write

jobs:
  automation:
    runs-on: ubuntu-latest
    steps:
      - name: Automation logic
        run: echo "Automating project..."
